spring:
  application:
    name: customer-app1
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: secret
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update